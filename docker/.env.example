# Credentials the n8n Docker container needs for the workflow Telegram nodes.
# Copy this file to docker/.env and fill in real values (docker/.env is gitignored).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
